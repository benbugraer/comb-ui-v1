import { Metadata } from "next";

export const metadata: Metadata = {
  title: "CombUI | Contact",
  description: "Lets contact with us.",
};

export default function ContactPage() {
  return <div>Contact Page</div>;
}
