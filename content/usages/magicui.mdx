---
title: "Magic UI"
description: "Open-source animated components built with React, Typescript, Tailwind CSS, and Framer Motion."
snippet: "npm install clsx tailwind-merge framer-motion"
snippet2: "yarn add clsx tailwind-merge framer-motion"
snippet3: "pnpm install clsx tailwind-merge framer-motion"
image: "/usages/magicui.jpg"
---

## Add Util File

Copy and paste the following code into your project.
Create `utils` folder in your project and after than create `util.ts` file in `utils` folder. In the `util.ts` paste this code.

```ts
// lib/utils.ts
import clsx, { ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```
